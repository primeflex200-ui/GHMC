# GitHub Pages Configuration for IALA Civic Services

# Site settings
title: "IALA Civic Services"
description: "Complete Digital Platform for IALA Cherlapally"
url: "https://primeflex200-ui.github.io"
baseurl: "/IALA"

# Build settings
markdown: kramdown
highlighter: rouge

# Plugins
plugins:
  - jekyll-sitemap
  - jekyll-feed

# Exclude files from processing
exclude:
  - node_modules/
  - package.json
  - package-lock.json
  - server.js
  - start-server.bat
  - .vscode/

# Include files
include:
  - _redirects

# Default layout
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"